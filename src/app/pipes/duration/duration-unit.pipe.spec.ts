import { DurationUnitPipe } from './duration-unit.pipe';

describe('DurationUnitPipe', () => {
  it('create an instance', () => {
    const pipe = new DurationUnitPipe();
    expect(pipe).toBeTruthy();
  });
});
