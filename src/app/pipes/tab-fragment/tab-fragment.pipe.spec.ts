import { TabFragmentPipe } from './tab-fragment.pipe';

describe('TabFragmentPipe', () => {
  it('create an instance', () => {
    const pipe = new TabFragmentPipe();
    expect(pipe).toBeTruthy();
  });
});
