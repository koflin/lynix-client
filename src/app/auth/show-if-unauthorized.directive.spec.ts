import { ShowIfUnauthorizedDirective } from './show-if-unauthorized.directive';

describe('ShowIfUnauthorizedDirective', () => {
  it('should create an instance', () => {
    const directive = new ShowIfUnauthorizedDirective();
    expect(directive).toBeTruthy();
  });
});
